# API Configuration
# Leave empty to use relative URLs (recommended for same-origin deployments)
VITE_API_BASE_URL=

# WebSocket Configuration
# Leave empty to use window.location.host (recommended for same-origin deployments)
VITE_WS_URL=

# Examples for different deployments:
# 
# Local Development (different port)
# VITE_API_BASE_URL=http://localhost:3000
# VITE_WS_URL=ws://localhost:3000
#
# Production (different domain)
# VITE_API_BASE_URL=https://api.tunnelforge.com
# VITE_WS_URL=wss://api.tunnelforge.com 